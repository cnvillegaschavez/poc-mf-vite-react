# Para auth-remote (Terminal 1)
   cd auth-remote
   npm run build && npm run preview

# Para products-remote (Terminal 2)
   cd products-remote
   npm run build && npm run preview

# Para online-shopping-host (Terminal 3)
   cd products-remote
   npm run dev
